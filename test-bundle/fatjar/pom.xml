<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.pointertrace.siglet.impl.test.bundle</groupId>
        <artifactId>siglet-container-test-bundle</artifactId>
        <version>0.0.12</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>fatjar-suffix-spanlet-test</artifactId>
    <name>test - spanlet fatjar bundle</name>
    <description>fatjar bundle to be used in integration tests</description>

    <dependencies>
        <dependency>
            <groupId>io.github.pointertrace.siglet.api</groupId>
            <artifactId>siglet-api</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.github.pointertrace.siglet.parser</groupId>
            <artifactId>siglet-yaml-parser</artifactId>
            <version>0.0.5</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.github.pointertrace.siglet.impl.test.bundle</groupId>
            <artifactId>internal-lib-test</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <finalName>${project.artifactId}-${project.version}-original</finalName>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.7.1</version>
                <dependencies>
                    <dependency>
                        <groupId>io.github.pointertrace.siglet.assembly</groupId>
                        <artifactId>siglet-assembly-config</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <descriptorRefs>
                        <descriptorRef>siglet-assembly-config</descriptorRef>
                    </descriptorRefs>
                    <finalName>${project.artifactId}</finalName>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
